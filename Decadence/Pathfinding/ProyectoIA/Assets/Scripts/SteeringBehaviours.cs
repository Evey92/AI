using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SteeringBehaviours : MonoBehaviour {

 //   public Vector3 Seek(Vector2 obj, Vector2 pos, float m)
 //   {
 //       int radio = 4;
 //       Vector3 force;
 //       if((pos - obj).magnitude <= radio)
 //   {
 //           float scale = mag / radio;
 //           force *= scale;
 //       }
 //       return force
 //}


    public Vector3 Seek(Vector3 obj, Vector3 pos, float m)
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }

    public Vector3 Flee()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }
    public Vector3 Evade()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }

    public Vector3 Pursue()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }
    public Vector3 Wander()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }

    public Vector3 Arrive()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }
    public Vector3 PathFollowing()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }

    public Vector3 Patrol()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }
    public Vector3 ObstacleAvoidance()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }

    public Vector3 Flocking()
    {
        Vector3 direccion = new Vector3();

        return direccion;
    }

    public class Boid
    {
        float velocidad;
        float radio;

    }
    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
